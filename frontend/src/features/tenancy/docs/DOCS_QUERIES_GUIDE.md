# TanStack Query Guide — Used in this slice

- Use TanStack Query for server state (lists, detail fetches, creates)
- Keep UI state in component state (modals, form inputs)
- Include orgSlug in query keys
- Use keepPreviousData for pagination smoothing
- Invalidate correct keys after mutations
